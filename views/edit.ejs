<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Edit Post BLOG-Express</title>
		<script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body>
		<div class="w-full min-h-screen bg-zinc-900 text-white p-10">
			<a href="/logout" class="text-sm float-right mb-5 bg-red-500 rounded-md px-3 py-2 inline-block cursor-pointer">Logout</a>

			<h5 class="mb-5">Edit Your Post</h5>
			<form action="/update/<%= post._id %>" method="post">
				<textarea name="content" placeholder="" class="p-3 block outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"><%= post.content %></textarea>
				<input type="submit" value="Update Post" class="text-sm bg-yellow-500 px-3 py-2 mt-2 w-40 block rounded-md cursor-pointer">
			</form>
		</div>
	</body>
</html>
